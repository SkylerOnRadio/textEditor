# Text Editor For Linux

This is my attempt at making a text editor in C++ for the terminal, from the scratch using as little libraries as possible. Currently the editor is still being built and is not at a usable place.

## Libraries Used

The program currently only uses the following libraries

- ncurses - a programming library for creating textual user interface(TUI) that works on a wide variety of terminals
